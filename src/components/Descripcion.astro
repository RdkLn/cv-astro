---

const calculateYearsDifference = (startDate: string) => {
    const currentDate = new Date();
    const start = new Date(startDate);
    const differenceInMilliseconds = currentDate.getTime() - start.getTime();
    return Math.floor(differenceInMilliseconds / (1000 * 60 * 60 * 24 * 365.25));
};

const yearsOfExperience = calculateYearsDifference('2022-03-01');
---

<!-- <main class="py-5"> -->
    <div class=" px-4 mx-auto titulo text-3xl max-w-2/5">Desarrollador de Software</div>
    <div class="bg-blue-800 px-4 mx-auto p-4 rounded-xl sm:max-w-3/5 md:max-w-2/5 max-w-4/5">
        <div class="grid grid-flow-col gap-4">
            <!-- <div class="md:col-span-2 col-span-1 font-extrabold text-4xl md:text-5xl">Desarrollador <span class="block invisible sm:visible"></span> de Software</div> -->
            <div class="md:col-span-2 col-span-1 row-span-2 ">
                <p>Desarrollador de software con <span class="text-yellow-300">+{yearsOfExperience} años de experiencia</span>, centrado en backend. Disfruto construyendo aplicaciones web que funcionen bien por dentro y se sientan bien por fuera. Me importa escribir código limpio, que escale sin dramas y que otros puedan entender sin sufrir. Siempre estoy buscando cómo mejorar, ya sea aprendiendo algo nuevo o afinando lo que ya sé. <span class=" block"/>Me ubico en Madrid, España 🇪🇸</p>
            </div>
        </div>
    </div>
<!-- </main> -->
<style>
	/* main {
		@apply grid place-content-center ;
        color: #fff;
	}
	main > div {
		@apply container;
	} */
    p{
        font-family: 'Roboto', sans-serif;
    }
    div{
        color: #fff;
    }
    .titulo{
        font-family: 'Poppins', sans-serif;
        color: #000;
        font-weight: 800;
    }
</style>
